import 'package:flutter/material.dart';

class MyDialogDemo extends StatelessWidget {
  const MyDialogDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
